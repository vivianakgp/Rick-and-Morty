import React from "react";

const SearchBox = ()=> {
    return (
        <div className="SearchBox">
            <input type="text" placeholder="type a location"></input>
            <button>search</button>

        </div>

    );
}
export default SearchBox;